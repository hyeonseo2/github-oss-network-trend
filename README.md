# GitHub OSS Network Trend

[English](./README.md) | [한국어](./README.ko.md)

Static OSS trend dashboard hosted on GitHub Pages.

This repository no longer uses BigQuery, Cloud Run, dbt, or Terraform.
Data is generated by GitHub Actions and published as static JSON files.

## 1. What this project does

- Tracks repository activity changes for 7D, 14D, 30D windows
- Builds shared-contributor network edges between repositories
- Serves interactive chart/table/network UI from static files

## 2. Current architecture

```text
GitHub REST API
    |
    v
GitHub Actions (scheduled + manual)
    |
    v
scripts/build_static_data.py
    |
    v
docs/data/*.json
    |
    v
GitHub Pages (docs/index.html)
```

## 3. Repository layout

```text
open-source-ecosystem-analytics-platform/
├── .github/workflows/pages.yml
├── scripts/build_static_data.py
├── docs/
│   ├── index.html
│   ├── data/
│   │   ├── meta.json
│   │   ├── trend_7d.json
│   │   ├── trend_14d.json
│   │   ├── trend_30d.json
│   │   ├── network_7d.json
│   │   ├── network_14d.json
│   │   ├── network_30d.json
│   │   └── top_repos.json
│   ├── github_pages_deploy.md
│   ├── operations_runbook.md
│   └── de_zoomcamp_project_document.md
├── .env.example
├── Makefile
└── README.md
```

## 4. Data outputs

- `meta.json`: build metadata
- `trend_7d/14d/30d.json`: ranked trend rows by window
- `network_7d/14d/30d.json`: network nodes/edges by window
- `top_repos.json`: top rows snapshot for quick usage

## 5. Metric definitions

- `Activity Δ` = current-window events - previous-window events
- `Contributor Δ` = current-window unique contributors - previous-window unique contributors
- `Trend Score` = `Activity Δ + 2 * Contributor Δ`
- `shared_contributor_count` = count of contributors active in both repositories for the selected window

## 6. Local usage

```bash
cp .env.example .env
source .env

make build-data
make run-site
```

Local URL: `http://127.0.0.1:8080`

## 7. GitHub Actions data refresh

Workflow: `.github/workflows/pages.yml`

Triggers:

- Daily schedule (UTC)
- Manual run (`workflow_dispatch`)
- Push to `main` for docs/script/workflow changes

Each run:

1. Calls GitHub API
2. Rebuilds `docs/data/*.json`
3. Deploys `docs/` to GitHub Pages

## 8. Configuration

Set via `.env` (local) or workflow env:

- `GITHUB_TOKEN` (recommended)
- `MAX_REPOS`
- `TREND_TOP_N`
- `NETWORK_MAX_EDGES`
- `MIN_SHARED_COUNT`
- `REQUEST_SLEEP_MS`
- `HTTP_TIMEOUT_SECONDS`
- `EVENT_MAX_PAGES`

## 9. Known limitations

- Network quality depends on GitHub API rate limits and available event history
- Without token, API limits are low and some repos can be skipped
- Static snapshots are batch-based, not real-time

## 10. Documentation

- [GitHub Pages deployment guide](docs/github_pages_deploy.md)
- [Operations runbook](docs/operations_runbook.md)
- [DE Zoomcamp project document](docs/de_zoomcamp_project_document.md)
