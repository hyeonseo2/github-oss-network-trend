# GCP
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=asia-northeast3
BQ_LOCATION=US

# Terraform backend bucket for state
TF_STATE_BUCKET=your-terraform-state-bucket

# dbt (for local execution)
DBT_PROFILES_DIR=./dbt
